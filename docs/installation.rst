.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install gbrs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gbrs
    $ pip install gbrs
